build:
  docker:
    web: docker/backend/Dockerfile
run:
  web: bundle exec puma -C backend/config/puma.rb
release:
  web: web
  command:
    - bin/rails db:migrate &&
      bin/rails db:seed &&
      bin/rails db:example
